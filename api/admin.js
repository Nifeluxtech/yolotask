import { adminClient,requireUser,rpc } from '../src/server/supabase.js'; import { ok,fail,body } from '../src/server/http.js'; import { requiredString,oneOf,positiveInt } from '../src/server/validation.js';
export default async function handler(req,res) { try { const url=new URL(req.url,`http://${req.headers.host||'localhost'}`); const action=url.searchParams.get('action')||'dashboard'; const {profile}=await requireUser(req,['admin']); if(action==='dashboard' && req.method==='GET') { const {data,error}=await adminClient.from('admin_dashboard').select('*').single(); if(error) throw error; return ok(res,{dashboard:data}); } if(action==='users' && req.method==='GET') { const roleFilter=url.searchParams.get('role'); const q=url.searchParams.get('q'); let query=adminClient.from('profiles').select('id,full_name,email,role,is_activated,is_suspended,reputation_rank,xp,created_at').order('created_at',{ascending:false}).limit(100); if(roleFilter && ['earner','advertiser','admin'].includes(roleFilter)) query=query.eq('role',roleFilter); if(q) query=query.ilike('full_name',`%${q}%`); const {data,error}=await query; if(error) throw error; return ok(res,{users:data||[]}); } if(action==='user-status' && req.method==='PATCH') { const input=await body(req); const targetId=requiredString(input.profile_id,'Profile ID',80); const suspended=Boolean(input.is_suspended); const {data,error}=await adminClient.from('profiles').update({is_suspended:suspended}).eq('id',targetId).select('id,is_suspended').single(); if(error) throw error; await adminClient.from('audit_logs').insert({actor_id:profile.id,action:suspended?'user_suspended':'user_unsuspended',entity_type:'profile',entity_id:targetId}); return ok(res,{user:data}); } if(action==='settings' && req.method==='GET') { const {data,error}=await adminClient.from('platform_settings').select('*').order('key',{ascending:true}); if(error) throw error; return ok(res,{settings:data||[]}); } if(action==='settings' && req.method==='PATCH') { const input=await body(req); const key=requiredString(input.key,'Setting key',120); const value=requiredString(String(input.value),'Setting value',1000); const {data,error}=await adminClient.from('platform_settings').upsert({key,value,updated_by:profile.id}).select().single(); if(error) throw error; return ok(res,{setting:data}); } if(action==='interests' && req.method==='POST') { const input=await body(req); const {data,error}=await adminClient.from('interests').insert({name:requiredString(input.name,'Interest name',120),category_id:input.category_id||null}).select().single(); if(error) throw error; return ok(res,{interest:data}); } if(action==='announcement' && req.method==='POST') { const input=await body(req); const {data,error}=await adminClient.from('announcements').insert({title:requiredString(input.title,'Title',160),message:requiredString(input.message,'Message',5000),audience:oneOf(input.audience||'all','Audience',['all','earners','advertisers']),created_by:profile.id}).select().single(); if(error) throw error; return ok(res,{announcement:data}); } return fail(res,405,'Admin action not supported.'); } catch(error) { return fail(res,error.status||400,error.message||'Admin request failed.'); } }
