import { adminClient,requireUser,rpc } from '../src/server/supabase.js'; import { ok,fail,body } from '../src/server/http.js'; import { positiveInt,requiredString,oneOf } from '../src/server/validation.js';
export default async function handler(req,res) { try { const action=new URL(req.url,`http://${req.headers.host||'localhost'}`).searchParams.get('action')||'create'; const {profile}=await requireUser(req,['earner','admin']); if(action==='create' && req.method==='POST') { const input=await body(req); const result=await rpc('request_withdrawal',{p_profile_id:profile.id,p_amount:positiveInt(input.amount,'Amount'),p_bank:requiredString(input.bank,'Bank',120),p_account_number:requiredString(input.account_number,'Account number',20),p_account_name:requiredString(input.account_name,'Account name',160)}); return ok(res,{withdrawal:result}); } if(action==='review' && req.method==='PATCH' && profile.role==='admin') { const input=await body(req); const result=await rpc('review_withdrawal',{p_admin_id:profile.id,p_withdrawal_id:requiredString(input.withdrawal_id,'Withdrawal ID',80),p_status:oneOf(input.status,'Status',['approved','rejected','processing','paid']),p_reason:input.reason||null}); return ok(res,{withdrawal:result}); } return fail(res,405,'Withdrawal action not supported.'); } catch(error) { return fail(res,error.status||400,error.message||'Withdrawal request failed.'); } }
