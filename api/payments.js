import { adminClient,requireUser } from '../src/server/supabase.js'; import { ok,fail,body } from '../src/server/http.js'; import { positiveInt } from '../src/server/validation.js';
export default async function handler(req,res) { try { const action=new URL(req.url,`http://${req.headers.host||'localhost'}`).searchParams.get('action'); if(action==='initialize' && req.method==='POST') { const {profile}=await requireUser(req,['earner','advertiser']); const input=await body(req); const amount=profile.role==='earner'?1000:positiveInt(input.amount,'Amount'); const reference=`YOTO-${profile.id.slice(0,8)}-${Date.now()}`; const response=await fetch('https://api.paystack.co/transaction/initialize',{method:'POST',headers:{Authorization:`Bearer ${process.env.PAYSTACK_SECRET_KEY}`,'Content-Type':'application/json'},body:JSON.stringify({email:profile.email,amount:amount*100,reference,callback_url:`${process.env.APP_URL}/app?payment=verify&reference=${reference}`,metadata:{profile_id:profile.id,purpose:profile.role==='earner'?'activation':'wallet_funding',amount_naira:amount}})}); const payload=await response.json(); if(!response.ok || !payload.status) throw new Error('Payment initialization failed.'); await adminClient.from('payment_intents').insert({profile_id:profile.id,reference,purpose:profile.role==='earner'?'activation':'wallet_funding',amount_kobo:amount*100,status:'initialized'}); return ok(res,{authorization_url:payload.data.authorization_url,reference}); }
if(action==='verify' && req.method==='POST') { const {profile}=await requireUser(req,['earner','advertiser']); const input=await body(req); if(typeof input.reference!=='string') throw new Error('Payment reference is required.'); const response=await fetch(`https://api.paystack.co/transaction/verify/${encodeURIComponent(input.reference)}`,{headers:{Authorization:`Bearer ${process.env.PAYSTACK_SECRET_KEY}`}}); const payload=await response.json(); if(!response.ok || payload.data?.status!=='success') throw new Error('Payment could not be verified.'); const {data:intent}=await adminClient.from('payment_intents').select('*').eq('reference',input.reference).eq('profile_id',profile.id).single(); if(!intent || intent.status==='verified') return ok(res,{verified:true}); if(Number(payload.data.amount)!==Number(intent.amount_kobo)) throw new Error('Verified amount does not match the payment intent.'); const result=await adminClient.rpc('settle_verified_payment',{p_reference:input.reference,p_paystack_id:String(payload.data.id)}); if(result.error) throw result.error; return ok(res,{verified:true,result:result.data}); }
return fail(res,405,'Payment action not supported.'); } catch(error) { return fail(res,error.status||400,error.message||'Payment request failed.'); } }
